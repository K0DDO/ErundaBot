---
description: Git commit messages — subject only, no body
alwaysApply: true
---

# Коммиты

- Одна строка — только subject (`git commit -m "message"`).
- **Никогда** не добавляй body, второй `-m`, HEREDOC с абзацами или описание «почему».
- Стиль: lowercase, без точки в конце.
